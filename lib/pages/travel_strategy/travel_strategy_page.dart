import 'package:flutter/widgets.dart';

class TravelStrategyPage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("我是旅游攻略页面"),
    );
  }
}
